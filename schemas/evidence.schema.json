{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "required": ["task_id", "run_id", "commit", "methodology_ref", "checks", "reviews", "attempts", "artifact", "deployment", "status", "created_at"],
  "properties": {
    "task_id": { "type": "string", "pattern": "^TASK-[0-9]{4,}$" },
    "run_id": { "type": "string", "minLength": 1 },
    "commit": { "type": "string", "pattern": "^[0-9a-f]{7,40}$" },
    "methodology_ref": { "type": "string", "minLength": 1, "not": { "const": "latest" } },
    "checks": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/$defs/result" }
    },
    "reviews": {
      "type": "array",
      "items": { "$ref": "#/$defs/result" }
    },
    "attempts": { "type": "integer", "minimum": 1, "maximum": 3 },
    "artifact": { "type": "string", "minLength": 1 },
    "deployment": {
      "type": "object",
      "additionalProperties": false,
      "required": ["environment", "probes"],
      "properties": {
        "environment": { "type": "string", "minLength": 1 },
        "probes": {
          "type": "array",
          "minItems": 1,
          "items": { "$ref": "#/$defs/result" }
        }
      }
    },
    "status": { "enum": ["passed", "failed", "held"] },
    "created_at": { "type": "string", "format": "date-time" }
  },
  "$defs": {
    "result": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "status"],
      "properties": {
        "name": { "type": "string", "minLength": 1 },
        "status": { "enum": ["passed", "failed", "not_applicable"] },
        "reason": { "type": "string", "minLength": 1 }
      },
      "allOf": [
        {
          "if": { "properties": { "status": { "const": "not_applicable" } }, "required": ["status"] },
          "then": { "required": ["reason"] }
        }
      ]
    }
  }
}
