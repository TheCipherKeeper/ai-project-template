# Верификация (verification gate) — референс

Автоматическая проверка соответствия репозитория канону на каждый коммит.

> Это референс (полная теория: рёбра графа, conformance/behavioral, полный
> invariant-чеклист, триггер/политики).
> Процедура «проверить перед коммитом» — компактно в `docs/guide/40-verify.md`.

**Идея:** на изменение в узле графа проверять все рёбра, инцидентные этому узлу
— **вверх** (узел соответствует авторитету) и **вниз** (все дочерние
соответствуют ему). Это закрывает две проблемы: enforcement (правила
проверяются, а не существуют на бумаге) и рассинхронизацию копии с
первоисточником (правила читаются из авторитета, не из копии; каждое изменение
— коммит, который триггерит нужное ребро).

> Этот репо — **методология** (корень авторитета в графе). Для него активно
> только ребро **вниз** (`методология → хаб` + `методология → все интерфейсы` +
> `методология → все stub-таргеты`; сервисы — транзитно через хаб): при изменении
> канона все инстанции перепроверяются. Ребро «вверх» у корня
> нет (вышего авторитета нет), но гейт самого этого репо — **docs-verify**
> (проверка доков/скелетов, см. корневой `AGENTS.md`). Правило обязано быть
> единым по всему графу, иначе гейт не композируется.

## Рёбра и направления

| Где коммит | Проверяемые рёбра | Что проверяется |
|---|---|---|
| Методология (этот репо) | вниз: `методология → хаб` + `методология → все интерфейсы` + `методология → все stub-таргеты` | все инстанции соответствуют обновлённому канону (сервисы — через хаб) |
| Хаб | вверх: `методология → хаб`; вниз: `хаб → все сервисы` + `хаб → все интерфейсы` + `хаб → все stub-таргеты` | хаб соответствует методологии; все сервисы соответствуют хабу; все интерфейсы и stub-таргеты соответствуют `COMPOSITION` |
| Сервис | вверх: `хаб → сервис`; presentation (только для gateway): `gateway → интерфейсы` | сервис соответствует хабу (`CONVENTIONS`); для gateway — его presentation-эндпоинты соответствуют тому, что заявляют интерфейсы (agent) |
| Интерфейс | вверх: `методология → интерфейс` + `хаб → интерфейс`; presentation: `gateway → интерфейс` | интерфейс соответствует методологии (структура/канон) и `COMPOSITION` (потребляет только существующие эндпоинты gateway); его заявленные вызовы соответствуют `ARCHITECTURE` gateway (agent) |
| Stub-таргет | вверх: `методология → stub` + `хаб → stub` | stub соответствует методологии (структура/канон репо-типа, без `MODULE`/`SPEC`) и `COMPOSITION` (перечислен как цель; stub не потребляет envelope). Out-of-band-наблюдение — не гейт-ребро |

Список детей для «вниз» — это `COMPOSITION.md` хаба (отдельный реестр не нужен).

## Источник правил — авторитет, не копия

Гейт читает правила из **авторитета** (методология/хаб), а не из копии в
проверяемом репо. Иначе копия, уплывшая от канона, проверяет сама себя и
расхождение не видит. Поэтому сервисы/хаб **не несут** `docs/guide/`/`docs/refs/`
и ссылаются на этот репо методологии, а не копируют их. Это ключевое условие
закрытия проблемы рассинхронизации.

## Conformance vs behavioral

| Тип | Что проверяется | Кто | Блокирует? | Разрешение находки |
|---|---|---|---|---|
| **behavioral** | собирается/тестируется ли сервис против контрактов | детерминированный CI (lint/test/build) | да | fail → чини, не коммить |
| **conformance** | структурное соответствие: доки, методология, присутствие контракта, топики, ADR-ссылки | LLM-агент | нет (один вердикт не блокирует) | adversarial-verify → авто-фикс (re-гейт) либо в `BACKLOG` + холд; «комментарий в воздух» не допускается |

Гейт-агент не делает behavioral — там нужна воспроизводимость билда, не
суждение LLM. Conformance не блокирует одним вердиктом (LLM недетерминирован,
false positive заблокирует реальную работу), но **разрешается**: находка,
прошедшая adversarial-verify (попытку опровергнуть), ведёт к авто-фиксу
(новый коммит, re-гейт) или записи в `BACKLOG` + холд PR. «Комментарий в воздух»
не допускается — в автономном цикле его некому читать (`docs/refs/PIPELINE.md`).
Агент-ошибка/недоступность не блокирует; путь апелляции (false positive)
обязателен — иначе доверие к гейту сдувается. Цикл фикс→recheck ограничен N
попытками (bounded retries); дальше — в `BACKLOG`, не зацикливаться.

> **Кто ревьюит.** В автономном цикле ревьюер — независимый вызов агента (не
> имплементер); человек в pre-deploy не участвует. Сам репо методологии —
> исключение: его гейт docs-verify, его PR'ы ревьюит человек (`AGENTS.md`).

## Invariant-чеклист

Без явного чеклиста «агент проверяет соответствие сверху вниз» — размытое
требование, которое агент трактует по-разному каждый раз (это и убивает
детерминизм). Чеклист обязателен.

| # | Инвариант | Тип | Блок. | Скоуп |
|---|---|---|---|---|
| 1 | Ветка `feat/<task>` (или main) | rule | да | дифф |
| 2 | Conventional Commits + scope | rule | да | дифф |
| 3 | lint/test/build проходят | rule (behavioral) | да | репо |
| 4 | Дифф тронул спеку «TODO→есть» → тронул код/тест модуля | rule | да | дифф |
| 5 | `[x]` в BACKLOG — на первом невыполненном пункте (без перепрыгивания) | rule | да | cross-cutting |
| 6 | Каждый пункт «Что есть» в спеке → есть тест | agent | нет | дифф+ |
| 7 | Содержимое «Что есть» соответствует коду | agent | нет | cross-cutting |
| 8 | ARCHITECTURE (роль/топики/граница) соответствует хабу (`CONVENTIONS`) | agent | нет | cross-cutting |
| 9 | Stub не выдаётся за реализацию (placeholder/TODO помечены) | agent | нет | cross-cutting |
| 10 | Доки не противоречат: внутри яруса (в т.ч. `guide/` против `refs/`) — дефект (чинят к одной правде), между ярусами — старший побеждает | agent | нет | cross-cutting |
| 11 | ADR-ссылки из ARCHITECTURE существуют в хабе | rule | да | cross-cutting |
| 12 | Модуль экспонирует каноничные швы (`usecases/`+`ports`+`domain`+`adapters/` по per-stack имён); каждый юзкейс определяет input port | rule (structural) | да | модуль |
| 13 | Направление зависимостей: usecases → только ports+domain, не adapters; adapters реализуют ports; модуль реэкспортит только input ports | agent | нет | модуль+ |
| 14 | Интерфейс в `COMPOSITION` хаба; заявленные им consumed-эндпоинты (из `ARCHITECTURE` интерфейса) существуют в `ARCHITECTURE` **gateway-сервиса** (единственного browser-facing surface) | agent | нет | cross-cutting |
| 15 | Безопасник: vuln-scan зависимостей/секретов проходит (rule, детерм.); нет очевидных auth/secret-injection/input-trust/доверие-граница дыр (threat-model, agent) | rule + agent | да (scan) / нет (threat-model) | cross-cutting |

`rule` — детерминированная проверка (скрипт/CI). `agent` — семантическая (LLM).
`cross-cutting` — требует чтения за пределами диффа. **Роли агентов**
(разработчик/тестировщик/ревьюер/безопасник), их lens, артефакты и место в
гейте — `docs/refs/ROLES.md`; тестировщик кормит #3 (тесты), безопасник —
#15. Канон швов модуля — `docs/refs/MODULE.md`; процедура заведения —
`docs/guide/20-define-module.md`. Канон интерфейса как клиента —
`docs/refs/COMMUNICATION.md` → *Клиентский край*; скелет — `skeletons/interface/`.
Канон stub-таргета как standalone-программы — `docs/refs/COMMUNICATION.md` →
*Stub-таргет*; скелет — `skeletons/stub/`.

## Применимость инвариантов по типу репо

Чеклист сервисоцентричен, но на него ссылается и интерфейс, и stub-таргет —
поэтому явно:

- **Сервис:** весь чеклист (#1–15). `gateway-сервис` — тоже сервис (весь
  чеклист), но **цель** #14 (presentation-эндпоинты, потребляемые интерфейсами)
  применима **только** к нему (см. `COMMUNICATION` → *gateway-сервис*), а
  **#15 (безопасник)** для gateway обязателен (доверительная граница).
  У обычных сервисов #14-цель N/A; их клиентский край — топики брокера,
  потребляемые gateway (проверяет ребро `хаб → сервис`, инвариант #8).
- **Интерфейс:** #1, 2, 3, 9, 10, 11 (ветвление/коммиты, lint/test/build, stub,
  противоречие доков, ADR-ссылки) + #14 (регистрация в `COMPOSITION` и
  соответствие заявленных consumed-эндпоинтов `ARCHITECTURE` **gateway-сервиса**)
  + #15-частично (безопасник: XSS, секреты в браузере, `VITE_`-токены).
  `MODULE.md`/`SPEC.md` (#4–8, 12, 13) — бэкенд-канон, к интерфейсу не
  применяются.
- **Stub-таргет:** #1, 2, 3, 9, 10, 11 (те же «общие»: ветвление/коммиты,
  lint/test/build, stub, противоречие доков, ADR-ссылки) + #15 (безопасник по
  поверхностям, если есть). Без сервисных (#4–8, 12, 13 —
  `MODULE`/`SPEC`/BACKLOG/specs/швы не применяются: stub параметризуется
  дескриптором, не юзкейсами) и без интерфейсных (#14 — у stub нет
  consumed-эндпоинтов). Stub не потребляет envelope, хаб-контракт к нему
  неприменим. Регистрация stub'а в `COMPOSITION` — как #14 для интерфейса, agent.

## Базовый аудит

Перед включением гейта репозитории уже могли дрейфовать. Нужен одноразовый
полный проход `хаб → все сервисы` (неблокирующий), фиксирующий baseline и
список расхождений. Иначе первый же коммит хаба зажжёт всё красным.

## Триггер и политики

- Триггер — **на каждый коммит**, включая merge-commit и прямой push в
  `main`, иначе дрейф вносится в обход гейта.
- Детерминированные проверки (#1, 2, 3, 4, 5, 11, 12, 15-scan) — блокируют.
- Агентские conformance-проверки (#6, 7, 8, 9, 10, 13, 14, 15-threat-model) —
  не блокируют одним вердиктом; survived-находка разрешается (авто-фикс /
  `BACKLOG`+холд). Ошибка/недоступность агента **не** блокирует. Цикл
  фикс→recheck — bounded; N попыток, дальше — в `BACKLOG`.
- Каденс: rule — на push (дёшево); agent — на PR (готов к ревью), не на каждый
  коммит.
- **Auto-rc (автономный цикл):** merge в `main` → авто-тег `vX.Y.Z-rc.N` →
  деплой в тестовую среду. Тестовая среда — первая точка человека (баг →
  `BACKLOG`). Релизная процедура — `docs/guide/70-release.md`; модель цикла —
  `docs/refs/PIPELINE.md`.