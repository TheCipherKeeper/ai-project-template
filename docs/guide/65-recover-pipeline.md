# Этап 65 — Восстановить автономный pipeline

Сверить `[~]`, `run_id`, PR, commit, CI и deploy по `RECOVERY.md`. Продолжить
тот же run только идемпотентным шагом. При недоказуемом состоянии остановиться,
сохранить evidence и освободить лок отдельным recovery-PR; захват нового пункта
до этого запрещён.
