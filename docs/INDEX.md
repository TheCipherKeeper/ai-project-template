# docs/INDEX — карта документации

Точка входа. Прочитай этот файл первым, потом грузи нужный спек.

## Порядок чтения

1. Хаб (если есть) — `COMPOSITION.md` (состав программы), `CONVENTIONS.md`
   (event envelope), `adr/` (ADR). Системный контекст — там.
2. `AGENTS.md` — правила работы в репозитории.
3. `INDEX.md` (этот файл) — карта.
4. `BACKLOG.md` — очередь задач.
5. `ARCHITECTURE.md` — архитектура сервиса (модули, брокер, потоки, граница).
6. `LAYOUT.md` — раскладка каталогов репозитория (workspace модулей).
7. `DEPLOYMENT.md` — Dockerfile + локальный compose.
8. `STACKS.md` — toolchain и layout выбранного стека.
9. `specs/<module>.md` — контракт нужного модуля.

## Файлы

| Файл | Что описывает |
|---|---|
| AGENTS.md | Правила: ветвление, что можно/нельзя, коммиты, ADR, команды по стекам |
| BACKLOG.md | Очередь задач с приоритетами и зависимостями |
| ARCHITECTURE.md | Сервис: модули, брокер, потоки данных, граница доверия |
| LAYOUT.md | Раскладка каталогов: workspace модулей, спеки, общее |
| DEPLOYMENT.md | Dockerfile, локальный compose (брокер + сервис), ссылка на хаб-деплой |
| STACKS.md | Toolchain, layout и команды для Python/Go/Rust/TS |
| specs/<mod>.md | Контракты модулей сервиса — по одному на модуль |
| adr/*.md | ADR (если нет хаба; иначе — в хабе) |

> Список спеков заполни под свои модули. Удали `specs/EXAMPLE.md`.

## Как работать со спеками

- Прочитай `ARCHITECTURE.md` перед работой с любым модулем.
- Прочитай спек модуля перед изменением кода в нём.
- После реализации — обнови спек: перенеси пункт из «Что TODO» в «Что есть».
- Каждый пункт «Что есть» в спеке → должен иметь тест.
- Каждый спек следует структуре: описание / Интерфейсы / Типы / Что есть /
  Что TODO / Ограничения / Зависимости.
- Спек описывает контракт (что), не реализацию (как).
- Как соотнести модуль с каталогами выбранного стека — см. `LAYOUT.md`.

## Проверка перед коммитом

Три группы команд, все должны пройти:

- **lint** — форматирование и линтер.
- **test** — тесты.
- **build** — сборка.

Команды выбранного стека — в `AGENTS.md` → *Команды проверки по стекам* и в
`STACKS.md`.

Если любая fail — чини, не коммить.