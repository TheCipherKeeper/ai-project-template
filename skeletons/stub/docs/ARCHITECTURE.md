# Архитектура автономного компонента

> Шаблон одного автономного компонента. Компонент выполняется как отдельная
> программа, не подключается к брокеру и не является сервисом системы. Форма
> задаётся системой
> управления: контейнер, программа командной строки или иной вариант (`form`).
> Способ развёртывания зависит от формы: контейнер —
> `Dockerfile`; CLI — артефакт/сборка; `<methodology-repo>/docs/refs/DEPLOYMENT.md`).
> При наличии наблюдаемой поверхности сборщик контролирует её по отдельному
> каналу (см. `<methodology-repo>/docs/refs/COMMUNICATION.md` → *Автономный компонент*).
> `MODULE.md`/`SPEC.md` сюда **не применяются** (бэкенд-канон usecase-швов сервиса;
> компонент параметризуется, а не вариантами использования). Заполните секции
> под конкретный компонент.

## Что это

<!-- 1–2 предложения: назначение компонента и его место в системе.
     Системный контекст — хаб COMPOSITION.md. -->

## Что делает

<!-- Нумерованный список: что за standalone-программа, форма (form), что
     изображает/делает, чем параметризуется (дескриптор). Если наблюдаются — какие
     поверхности и как collector наблюдает их out-of-band. -->

## Чего не делает

<!-- Явные границы. Важно для агентов. -->

- Не подключается к брокеру и не публикует или читает его темы.
- Не является микросервисом.

## Поверхности (если наблюдаются)

<!-- Только когда есть наблюдаемая поверхность (collector наблюдает её out-of-band).
     Не presentation-эндпоинты. -->

| Поверхность | Протокол | Порт | Назначение |
|---|---|---|---|
| `<surface>` | TCP / SSH / HTTP | … | … |

## Параметризация

<!-- Чем параметризуется stub из control-plane: form (container|cli|…) и — при
     наблюдении — параметры поверхности (протокол/порты/баннер и т.п.). -->

## Доверительная граница

<!-- Где проходит граница автономного компонента: что находится по каждую сторону, какие действуют гарантии,
     подпись/аутентификация наблюдений collector'а. -->

## Деплой

- Деплой — по форме. `form=container`: контейнер со своим `Dockerfile`, локальная
  разработка — `docker-compose.yml` (этот stub, **без брокера**). `form=cli`:
  артефакт/сборка. Детали — `<methodology-repo>/docs/refs/DEPLOYMENT.md`, запуск —
  `<methodology-repo>/docs/guide/50-deploy.md`.
- Системный compose (все сервисы + цели) — в хабе, не здесь.
- Структурное соответствие методологии проверяется контрольным рубежом
  (`<methodology-repo>/docs/refs/VERIFICATION.md`).

## Ссылки

- Хаб `COMPOSITION.md` — состав системы, включая автономные компоненты.
- Хаб `adr/` — архитектурные решения программы.
- `<methodology-repo>/docs/refs/COMMUNICATION.md` → *Автономный компонент* —
  место компонента в
  модели общения.
