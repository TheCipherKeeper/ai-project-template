# Архитектура интерфейса

> Скелет одного интерфейса (React/TS). Интерфейс — клиент на доверительной
> границе, зовёт presentation-эндпоинты **gateway-сервиса** по HTTP/WS (модель —
> `<methodology-repo>/docs/ARCHITECTURE.md` → *Коммуникация*).
> Команды — `<methodology-repo>/docs/REFERENCE.md`; деплой —
> `<methodology-repo>/docs/OPERATIONS.md`. Заполни секции
> под свой интерфейс. `MODULE.md`/`SPEC.md` сюда **не применяются** (бэкенд-канон).

## Что это

<!-- 1–2 предложения: что за интерфейс, какие визуализации, для какой части
     программы. Системный контекст — хаб COMPOSITION.md. -->

## Потребление

> Манифест вызываемых presentation-эндпоинтов **gateway-сервиса**. **Обязателен**
> — это то, что сверяет гейт-agent #14
> contract tests с `ARCHITECTURE` **gateway**.
> Эндпоинты должны существовать и совпадать по версии с тем, что документировано
> в gateway. Интерфейс потребляет только gateway.

| Gateway | Эндпоинт | Метод | Версия | Формат ответа | Назначение |
|---|---|---|---|---|---|
| `<gateway>` | `/v1/<endpoint>` | GET / WS | v1 | JSON | … |
| `<gateway>` | `/v1/<endpoint>` | POST | v1 | JSON | … |

## Страницы / роуты

<!-- Карта страниц/экранов интерфейса. -->

| Роут | Что показывает | Откуда данные (gateway/эндпоинт) |
|---|---|---|
| `/` | … | `<gateway> /v1/...` |

## Компонентная структура

<!-- Краткий скетч: ключевые компоненты/хуки/сторы. Внутренняя фронтенд-архитектура —
     на усмотрение (React-конвенции); канон MODULE.md/SPEC.md не применяется. -->

- `pages/<Page>` — …
- `components/<Widget>` — …
- `hooks/<useThing>` — …

## Доверительная граница

<!-- Где проходит граница интерфейса: что он аутентифицирует, какие токены,
     CORS, что не делает (не лезет в чужую БД/брокер). Для интерфейсов без
     особой границы — секцию убрать. -->

## Ссылки

- Хаб `COMPOSITION.md` — состав программы (сервисы, вкл. gateway, + интерфейсы).
- `ARCHITECTURE` **gateway-сервиса** → *Доверительная граница* — документированные
  presentation-эндпоинты, которые этот интерфейс зовёт.
- Хаб `adr/` — архитектурные решения программы.
